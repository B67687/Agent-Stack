---
description: Model routing policy for opencode + OMO agents — which model each agent/category uses and why
globs: ["**/oh-my-openagent.jsonc", "**/opencode.jsonc", "**/.omo/plans/**", "**/.opencode/rules/model-routing.mdc"]
alwaysApply: false
---

# Model Routing (opencode-go landscape, muse-spark-primary 2026-08-27)

## Canonical relationship

This file is the always-injected routing policy. The live implementation lives in
`~/.omo/omo.jsonc` (agents map, categories map,
background_task.modelConcurrency). `opencode.jsonc` base model/small_model/agents are
untouched by routing. **Where this file and the config differ, the config wins** (it
executes); this file records intent so future edits stay justified.

## Routing table (agent → model → fallback)

| Agent | Model | Fallback |
|---|---|---|
| sisyphus | `opencode-go/muse-spark-1.2-contributor` | `mimo-v2.5 → gpt-5.6-luna → mimo-v2.5-free` |
|| architect / build / worker | `opencode-go/muse-spark-1.2-contributor` | `mimo-v2.5 → gpt-5.6-luna → mimo-v2.5-free` |
|| oracle / prometheus / metis / momus | `opencode-go/muse-spark-1.2-contributor` | `mimo-v2.5 → gpt-5.6-luna → mimo-v2.5-free` |
|| explore / scout / librarian / multimodal-looker | `opencode-go/muse-spark-1.2-contributor` | `mimo-v2.5 → gpt-5.6-luna → mimo-v2.5-free` |
|| writing / git / quick / unspecified-low / artistry / deep | `opencode-go/muse-spark-1.2-contributor` | `mimo-v2.5 → gpt-5.6-luna → mimo-v2.5-free` |
|| sisyphus-junior / unspecified-high | `opencode-go/muse-spark-1.2-contributor` | `mimo-v2.5 → gpt-5.6-luna → mimo-v2.5-free` |
|| test-writer / review | `opencode-go/gpt-5.6-luna` | `deepseek-v4-flash → minimax-m3 → mimo-v2.5-free` |
|| hephaestus / atlas | `opencode-go/deepseek-v4-flash` | (existing; both disabled) |
|| visual-engineering | `opencode-go/minimax-m3` | `gpt-5.6-luna → mimo-v2.5-free` |
|| general | category `unspecified-low` | (inherits) |

## Category table

| Category | Model |
|---|---|
|| ultrabrain | `opencode-go/muse-spark-1.2-contributor` |
|| unspecified-high | `opencode-go/muse-spark-1.2-contributor` |
|| artistry / deep / writing / git / quick / unspecified-low | `opencode-go/muse-spark-1.2-contributor` |

## Model rationale

### Muse Spark 1.2 Contributor as primary workhorse (routed 2026-08-26, expanded 2026-08-27)

- **Cost**: $0.10 input / $0.20 output per 1M tokens (~$0.01/task)
- **Intelligence**: AA Intelligence Index **57** (#17/187)
- **Privacy**: ⚠️ Meta trains on prompts and completions — GitHub identity risk
- **Rate limit**: 45,300 req/5h (highest on platform)
- **Used on**: ALL agents except review, test-writer, visual-engineering (sisyphus now also on muse-spark — full Intelligence 57)
- **Why Muse Spark for these**: Intelligence 57 at $0.01/task is unbeatable value. All agents including orchestrator now run Intelligence 57.
- **Fallback chain**: muse-spark ($0.01) → mimo-v2.5 ($0.06) → gpt-5.6-luna ($0.05) → mimo-v2.5-free ($0, trains)

### MiMo-V2.5 as orchestrator guard (routed 2026-08-26, narrowed 2026-08-27)

- **Cost**: $0.14 input / $0.28 output per 1M tokens (~$0.06/task)
- **Intelligence**: AA Intelligence Index **38** (#24/107)
- **Privacy**: ✅ Zero-retention, no training
- **Rate limit**: 30,100 req/5h
- **Used on**: now FALLBACK only (no primary) — cleared 2026-08-28, sisa moved to muse-spark for Intelligence 57
- **Why MiMo-V2.5 now fallback**: Was orchestrator guard for privacy (zero-retention), but Intelligence 57 beats 38 on every metric and `agent-session kill` now clears stale cache. MiMo remains as first fallback after muse-spark.
- **Fallback chain**: muse-spark ($0.01) → mimo-v2.5 ($0.06) → gpt-5.6-luna ($0.05) → mimo-v2.5-free ($0, trains)
- **Data training**: paid tier = zero-retention. Free variant (hy3-free) trains on data.

### DeepSeek V4 Flash — quality fallback only

- AA Intelligence Index **52** (#5/107), fastest model (125 t/s, 1.05s TTFT)
- $0.44/$1.32 per 1M tokens — expensive for volume use but justified for quality-critical agents
- Peak/off-peak pricing: peak hours (01:00-04:00, 06:00-10:00 UTC Mon-Fri) = 2× prices
- 7,600 req/5h at $30/mo budget cap
- Used only on oracle/prometheus when GLM-5.2 quota is exhausted, and on test-writer/review
- **No free variant exists** — `deepseek-v4-flash-free` does not exist on the platform

### GLM-5.2 — quality fallback only

- AA Intelligence Index **53** (#4/107), highest intelligence but expensive
- $1.40/$4.40 per 1M tokens, 880 req/5h
- Sits as fallback on reasoning/planning agents (oracle, prometheus, metis, momus, ultrabrain)
- Not primary — 35× more expensive per task than Hy3

### GPT 5.6 Luna — budget fallback

- AA Intelligence Index **52** (tied with Flash), $0.20/$1.20 per 1M tokens
- At max effort: 150s TTFT (always-on CoT, too slow for agents)
- At low effort: 1.61s TTFT, 132 t/s, Intelligence 34 — cheap and fast but weaker
- **Luna is served via OpenAI `/responses`, not chat/completions** — works only through opencode's BUILT-IN provider
- Used as second fallback across all chains

### MiniMax M3 — multimodal only

- AA Intelligence Index **45**, 124 t/s, 1.31s TTFT
- $0.30/$1.20 per 1M tokens, 3,200 req/5h
- Only model supporting image + video input — kept for visual-engineering slot

### MiMo-V2.5 — orchestrator guard only

- AA Intelligence Index **38** (#24/107), above median 27
- Paid tier: $0.14/$0.28, 30,100 req/5h — cheap but Intelligence 38 weaker than Muse Spark 57
- Free tier: trains on user data, used only as last-resort fallback
- Routed as fallback 2026-08-28: all primaries moved to Muse Spark (Intelligence 57). MiMo remains as first fallback (privacy-safe, zero-retention).
- Previously primary for all sensitive agents (Aug 26), narrowed to orchestrator-only after Muse Spark proved superior

### Muse Spark 1.2 Contributor — primary for all agents (except orchestrator)

- AA Intelligence Index **57** (#17/187), highest intelligence on the platform
- $0.10/$0.20 on OpenCode Go (10× cheaper than official Meta API $1.25/$4.25)
- **Meta trains on prompts and completions** — the discount is paid with user data
- Sustainable pricing unclear (promotional rate vs official 10× gap)
- User decision 2026-08-28: expanded to ALL agents including sisyphus. Intelligence 57 and speed beat MiMo on every metric. Stale tmp cache bug fixed (`agent-session kill` now clears `/cache/tmp/opencode/*/ .omo/omo.jsonc`).

### Hy3 — retired from routing

- Was Intelligence 42, $0.04/task, 80 t/s — dethroned for being too slow
- Removed from all model fields and fallback chains 2026-08-26
- Removed from model fields and fallback chains entirely
- If Hy3 free window ends or model deprecated: fallback auto-catches via MiMo-V2.5 primary

### fallback_models allowlist (2026-08-02/03, claude-opus-5 incident)

Every agent/category carries an allowlist suppressing OMO's HARDCODED AGENT_MODEL_REQUIREMENTS chain (sisyphus chain[0] was claude-opus-5/anthropic — fired on transient errors despite disabled_providers; see docs/INCIDENTS.md). Pattern: `fallback_models[0]` = primary model, tail = gradient from cheap to expensive.

### Budget layering

Three enforcement layers (deliberate, not a conflict): soft routing target ~$15/mo (falsification trigger above) → hard block via routing + `modelConcurrency` caps (per-model concurrency zeros out budget-blocked models; qwen3.7-max concurrency=0 since 2026-08-02) → monitoring ceiling $60/mo.

## NOT-list (do not do)

1. **NEVER add a custom `opencode-go` provider entry with `@ai-sdk/openai-compatible`** — it breaks gpt-5.6-luna (responses-only transport). The built-in opencode-go provider handles per-model transport.
2. No Kimi K3 ($3/$15), no Grok 4.5 ($2/$6) — spend stays below that tier.
3. No Qwen3.7 Max ($2.50/$7.50) — hallucination-prone (Akita v2 51/C); concurrency 0.
4. ~~No Muse Spark 1.2 Contributor as PRIMARY~~ — **OVERRIDDEN 2026-08-27→28**: Muse Spark is now primary for ALL agents (Intelligence 57 at $0.01/task). Sisyphus moved 2026-08-28 after cache fix. Privacy: Meta trains but no PII in agent contexts.
5. Routing scope only — no permission/hook/agent-definition/provider changes.
6. No non-opencode-go / non-deepseek providers.
7. No `opencode-go/deepseek-v4-pro` — still the Apr preview; not worth the spend.
8. **Ox Alpha: SKIP** — too slow (always-on CoT latency). Removed from routing entirely.
9. **Hy3: SKIP** — too slow (80 t/s). Dethroned 2026-08-26. Not in fallback chains.

## Refresh trigger

Lightweight periodic re-review (LANDSCAPE-lite): when the user senses the
landscape shifted, or a model is added/retired on opencode.ai/docs/go, or
falsification signals appear (frontier outputs no measurable gain ~2 weeks, main
agent 429-starvation, spend >$15/mo). Re-verify against the live model list
(https://opencode.ai/zen/go/v1/models) and models.dev before changing anything.

## Benchmark integrity (2026-08-03)

When comparing models during LANDSCAPE checks, cite ONLY contamination-resistant
benches. **SWE-bench Verified is DEAD as a signal** — OpenAI Frontier Evals
stopped reporting it (Apr 2026): audit found >60% of tasks unsolvable-as-written,
models reproduce gold patches VERBATIM from task ID, saturated at 93.9%;
any score >80% warrants harness/tool-access scrutiny. Do NOT cite it.

Trustworthy: SWE-bench Pro (contamination-resistant by design — GPL-copyleft
+ private repos, held-out/commercial subsets; frontier ~59% on Pro vs 80-95%
on Verified), LiveCodeBench (time-split, contamination-free by design), HLE +
PaperBench (novel/fail-safe design). Caveat-only: Terminal-Bench 2.1 (canary
"largely symbolic safeguard" per its own paper; interactive-CLI harder to game
but vendor-reported numbers like flash 0731's 82.7 need independent confirm),
BenchLM (aggregate), NL2Repo/Cybergym/Toolathlon/DSBench/DeepSWE (vendor-reported).
